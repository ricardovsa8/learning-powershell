#Operadores de rediceccionamiento
cmdlet *> file # Envía todos los flujos de salida a un archivo, sobrescribiendo el contenido existente
cmdlet *>> file # Envía todos los flujos de salida a un archivo, agregando al contenido existente
cmdlet *>&1 # Envía todos los flujos de salida a un flujo de salida exitoso